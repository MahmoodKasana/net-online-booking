@page
@model NetOnlineBooking.Pages.Booking.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].FristName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].PhoneNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].AppointmentData)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Booking) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FristName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AppointmentData)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
